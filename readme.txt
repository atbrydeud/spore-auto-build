spore-auto-build
================

Set of scripts used by Go Continious Integration agents.

To call build.sh use(from the root dir of the repo):
    bash -c "spore-auto-build/build.sh"


build.sh is called by go-agent

cr_build.sh is executed in the chrooted environment
